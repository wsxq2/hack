from base64 import b16encode,b64encode,b32encode
def base(x):
    """get the base according string x

    :x: string
    :returns: base

    """
    b16=x 
def main():
    with open('a.txt') as f:
        x=f.read()
    print x
    base_decode={'16': b16encode, '32': b32decode, '64': b64decode}
    for i in range(10):
        base_encode[base(x)]

if __name__ == "__main__":
    main()
